Two separate .py files were used with one handling the mapping via KLT and the other handling the actual navigation.
Although the navigation file (player.py) has SIFT methods, none of its features were actually used. The actual exploration is happening in the mapping.py file
which maps out the route from the start to the goal. 
Please just treat player.py as a base code to just navigate the map since we are using mapping.py to explore.
